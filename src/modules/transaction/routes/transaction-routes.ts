import { Router } from 'express'

export const transactionRoutes = Router()

transactionRoutes.post('/create', () => {})
transactionRoutes.post('/update', () => {})
transactionRoutes.post('/delete', () => {})
